name: Docker Build and Push

on:
  workflow_call:
    inputs:
      image:
        description: Full image name (e.g. ghcr.io/arkonis-dev/ark)
        required: true
        type: string
      dockerfile:
        description: Path to Dockerfile relative to context
        required: true
        type: string
      context:
        description: Docker build context path
        type: string
        default: .
      platforms:
        description: Comma-separated target platforms
        type: string
        default: linux/amd64,linux/arm64
      extra-repo:
        description: "org/repo to check out into extra-repo-path (optional)"
        type: string
        default: ""
      extra-repo-path:
        description: Local path for the extra repo checkout
        type: string
        default: ""
      checkout-path:
        description: Subdirectory to check out the caller repo into (optional)
        type: string
        default: ""
      tag-suffix:
        description: Suffix appended to the version tag (e.g. -runtime)
        type: string
        default: ""
    outputs:
      image-tag:
        description: The version tag that was pushed (e.g. 0.1.0)
        value: ${{ jobs.build.outputs.image-tag }}
    secrets:
      GHCR_TOKEN:
        required: true
      GHCR_USERNAME:
        required: true

env:
  GRYPE_VERSION: v0.110.0
  SCAN_IMAGE: scan-image:local

jobs:
  build:
    name: Build and push ${{ inputs.image }}
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.tag.outputs.version }}
    permissions:
      contents: read

    steps:
      - name: Checkout caller repo
        uses: actions/checkout@v6
        with:
          path: ${{ inputs.checkout-path || '.' }}

      - name: Checkout extra repo
        if: inputs.extra-repo != ''
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.extra-repo }}
          path: ${{ inputs.extra-repo-path }}

      - name: Extract version from tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4.0.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.0.0

      - name: Log in to GHCR
        uses: docker/login-action@v4.0.0
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build image for scanning (linux/amd64, no push)
        uses: docker/build-push-action@v7.0.0
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          platforms: linux/amd64
          push: false
          load: true
          tags: ${{ env.SCAN_IMAGE }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install grype
        run: |
          curl -sSfL https://get.anchore.io/grype | sudo sh -s -- -b /usr/local/bin ${{ env.GRYPE_VERSION }}
          grype version

      - name: Scan image for CVEs
        run: grype "${{ env.SCAN_IMAGE }}" --fail-on high --quiet

      - name: Build and push multi-arch
        uses: docker/build-push-action@v7.0.0
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          platforms: ${{ inputs.platforms }}
          push: true
          tags: |
            ${{ inputs.image }}:${{ steps.tag.outputs.version }}${{ inputs.tag-suffix }}
            ${{ inputs.image }}:latest${{ inputs.tag-suffix }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.tag.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
